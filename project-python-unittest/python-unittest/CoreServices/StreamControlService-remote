#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import StreamControlService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '   beginStreamSession(string sessionId, i64 ttl, string type,  allowedClientIpAddresses, string deviceId, string channelId, string startTimestamp, string endTimeStamp)'
  print '  bool keepStreamSessionAlive(string sessionId, i64 ttl,  allowedClientIpAddresses)'
  print '  bool endStreamSession(string sessionId)'
  print '   getActiveOutboundStreamList()'
  print '   getRecordedVideoList(string deviceId, string channelId, string startTimestamp, string endTimeStamp)'
  print '   getStorageStatus()'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = StreamControlService.Client(protocol)
transport.open()

if cmd == 'beginStreamSession':
  if len(args) != 8:
    print 'beginStreamSession requires 8 args'
    sys.exit(1)
  pp.pprint(client.beginStreamSession(args[0],eval(args[1]),args[2],eval(args[3]),args[4],args[5],args[6],args[7],))

elif cmd == 'keepStreamSessionAlive':
  if len(args) != 3:
    print 'keepStreamSessionAlive requires 3 args'
    sys.exit(1)
  pp.pprint(client.keepStreamSessionAlive(args[0],eval(args[1]),eval(args[2]),))

elif cmd == 'endStreamSession':
  if len(args) != 1:
    print 'endStreamSession requires 1 args'
    sys.exit(1)
  pp.pprint(client.endStreamSession(args[0],))

elif cmd == 'getActiveOutboundStreamList':
  if len(args) != 0:
    print 'getActiveOutboundStreamList requires 0 args'
    sys.exit(1)
  pp.pprint(client.getActiveOutboundStreamList())

elif cmd == 'getRecordedVideoList':
  if len(args) != 4:
    print 'getRecordedVideoList requires 4 args'
    sys.exit(1)
  pp.pprint(client.getRecordedVideoList(args[0],args[1],args[2],args[3],))

elif cmd == 'getStorageStatus':
  if len(args) != 0:
    print 'getStorageStatus requires 0 args'
    sys.exit(1)
  pp.pprint(client.getStorageStatus())

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()

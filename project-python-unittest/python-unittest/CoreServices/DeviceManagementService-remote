#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import DeviceManagementService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  string addModel(DeviceModel model)'
  print '  bool updateModel(DeviceModel model)'
  print '  bool deleteModel(string modelId)'
  print '   listModels()'
  print '  string addDevice(DeviceDetails device)'
  print '  bool updateDevice(DeviceDetails device)'
  print '  bool deleteDevice(string deviceId)'
  print '   listDevices(string filter)'
  print '  DeviceDetails getDevice(string deviceId)'
  print '  bool activateDevice(DeviceDetails device)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = DeviceManagementService.Client(protocol)
transport.open()

if cmd == 'addModel':
  if len(args) != 1:
    print 'addModel requires 1 args'
    sys.exit(1)
  pp.pprint(client.addModel(eval(args[0]),))

elif cmd == 'updateModel':
  if len(args) != 1:
    print 'updateModel requires 1 args'
    sys.exit(1)
  pp.pprint(client.updateModel(eval(args[0]),))

elif cmd == 'deleteModel':
  if len(args) != 1:
    print 'deleteModel requires 1 args'
    sys.exit(1)
  pp.pprint(client.deleteModel(args[0],))

elif cmd == 'listModels':
  if len(args) != 0:
    print 'listModels requires 0 args'
    sys.exit(1)
  pp.pprint(client.listModels())

elif cmd == 'addDevice':
  if len(args) != 1:
    print 'addDevice requires 1 args'
    sys.exit(1)
  pp.pprint(client.addDevice(eval(args[0]),))

elif cmd == 'updateDevice':
  if len(args) != 1:
    print 'updateDevice requires 1 args'
    sys.exit(1)
  pp.pprint(client.updateDevice(eval(args[0]),))

elif cmd == 'deleteDevice':
  if len(args) != 1:
    print 'deleteDevice requires 1 args'
    sys.exit(1)
  pp.pprint(client.deleteDevice(args[0],))

elif cmd == 'listDevices':
  if len(args) != 1:
    print 'listDevices requires 1 args'
    sys.exit(1)
  pp.pprint(client.listDevices(args[0],))

elif cmd == 'getDevice':
  if len(args) != 1:
    print 'getDevice requires 1 args'
    sys.exit(1)
  pp.pprint(client.getDevice(args[0],))

elif cmd == 'activateDevice':
  if len(args) != 1:
    print 'activateDevice requires 1 args'
    sys.exit(1)
  pp.pprint(client.activateDevice(eval(args[0]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()

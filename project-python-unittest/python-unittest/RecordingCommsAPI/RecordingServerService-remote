#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import RecordingServerService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  i64 getServerID()'
  print '   getDevices()'
  print '   getRecordingDevices()'
  print '  bool setGlobalConfiguration(string configurations)'
  print '  bool setDeviceConfiguration(i64 deviceId, i32 channelIndex, string configurations)'
  print '  bool startRecordingTask(i64 deviceId, string taskType, string taskInfo)'
  print '  bool stopRecordingTask(i64 deviceId)'
  print '  string getEventVideo(i64 deviceId, i32 channelIndex, string eventInfo)'
  print '   startStreamSession(string sessionId, i64 deviceId, i32 channelIndex, string streamInfo)'
  print '  bool stopStreamSession(string sessionId)'
  print '  bool setSessionAccessControl(string sessionId, i64 ttl,  allowedClientIpAddresses)'
  print '   getStreamList(i64 deviceId, i32 channelIndex, string streamInfo)'
  print '   getStreamClients(i64 deviceId, i32 channelIndex, string streamInfo)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = RecordingServerService.Client(protocol)
transport.open()

if cmd == 'getServerID':
  if len(args) != 0:
    print 'getServerID requires 0 args'
    sys.exit(1)
  pp.pprint(client.getServerID())

elif cmd == 'getDevices':
  if len(args) != 0:
    print 'getDevices requires 0 args'
    sys.exit(1)
  pp.pprint(client.getDevices())

elif cmd == 'getRecordingDevices':
  if len(args) != 0:
    print 'getRecordingDevices requires 0 args'
    sys.exit(1)
  pp.pprint(client.getRecordingDevices())

elif cmd == 'setGlobalConfiguration':
  if len(args) != 1:
    print 'setGlobalConfiguration requires 1 args'
    sys.exit(1)
  pp.pprint(client.setGlobalConfiguration(args[0],))

elif cmd == 'setDeviceConfiguration':
  if len(args) != 3:
    print 'setDeviceConfiguration requires 3 args'
    sys.exit(1)
  pp.pprint(client.setDeviceConfiguration(eval(args[0]),eval(args[1]),args[2],))

elif cmd == 'startRecordingTask':
  if len(args) != 3:
    print 'startRecordingTask requires 3 args'
    sys.exit(1)
  pp.pprint(client.startRecordingTask(eval(args[0]),args[1],args[2],))

elif cmd == 'stopRecordingTask':
  if len(args) != 1:
    print 'stopRecordingTask requires 1 args'
    sys.exit(1)
  pp.pprint(client.stopRecordingTask(eval(args[0]),))

elif cmd == 'getEventVideo':
  if len(args) != 3:
    print 'getEventVideo requires 3 args'
    sys.exit(1)
  pp.pprint(client.getEventVideo(eval(args[0]),eval(args[1]),args[2],))

elif cmd == 'startStreamSession':
  if len(args) != 4:
    print 'startStreamSession requires 4 args'
    sys.exit(1)
  pp.pprint(client.startStreamSession(args[0],eval(args[1]),eval(args[2]),args[3],))

elif cmd == 'stopStreamSession':
  if len(args) != 1:
    print 'stopStreamSession requires 1 args'
    sys.exit(1)
  pp.pprint(client.stopStreamSession(args[0],))

elif cmd == 'setSessionAccessControl':
  if len(args) != 3:
    print 'setSessionAccessControl requires 3 args'
    sys.exit(1)
  pp.pprint(client.setSessionAccessControl(args[0],eval(args[1]),eval(args[2]),))

elif cmd == 'getStreamList':
  if len(args) != 3:
    print 'getStreamList requires 3 args'
    sys.exit(1)
  pp.pprint(client.getStreamList(eval(args[0]),eval(args[1]),args[2],))

elif cmd == 'getStreamClients':
  if len(args) != 3:
    print 'getStreamClients requires 3 args'
    sys.exit(1)
  pp.pprint(client.getStreamClients(eval(args[0]),eval(args[1]),args[2],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()

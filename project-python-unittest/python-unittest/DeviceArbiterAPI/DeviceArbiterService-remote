#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.8.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.protocol import TBinaryProtocol

import DeviceArbiterService
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  bool ping()'
  print '  bool addDevice(i64 deviceId, string deviceKey, string serverType,  supportedTaskTypes, string deviceInfo, bool connectToServerNeeded)'
  print '  bool removeDevice(i64 deviceId)'
  print '   getDevices()'
  print '  bool isAlive(i64 deviceId)'
  print '  bool startTask(i64 deviceId, string taskType, string taskInfo)'
  print '  bool stopTask(i64 deviceId, string taskType)'
  print '  bool isTaskRunning(i64 deviceId, string taskType)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = DeviceArbiterService.Client(protocol)
transport.open()

if cmd == 'ping':
  if len(args) != 0:
    print 'ping requires 0 args'
    sys.exit(1)
  pp.pprint(client.ping())

elif cmd == 'addDevice':
  if len(args) != 6:
    print 'addDevice requires 6 args'
    sys.exit(1)
  pp.pprint(client.addDevice(eval(args[0]),args[1],args[2],eval(args[3]),args[4],eval(args[5]),))

elif cmd == 'removeDevice':
  if len(args) != 1:
    print 'removeDevice requires 1 args'
    sys.exit(1)
  pp.pprint(client.removeDevice(eval(args[0]),))

elif cmd == 'getDevices':
  if len(args) != 0:
    print 'getDevices requires 0 args'
    sys.exit(1)
  pp.pprint(client.getDevices())

elif cmd == 'isAlive':
  if len(args) != 1:
    print 'isAlive requires 1 args'
    sys.exit(1)
  pp.pprint(client.isAlive(eval(args[0]),))

elif cmd == 'startTask':
  if len(args) != 3:
    print 'startTask requires 3 args'
    sys.exit(1)
  pp.pprint(client.startTask(eval(args[0]),args[1],args[2],))

elif cmd == 'stopTask':
  if len(args) != 2:
    print 'stopTask requires 2 args'
    sys.exit(1)
  pp.pprint(client.stopTask(eval(args[0]),args[1],))

elif cmd == 'isTaskRunning':
  if len(args) != 2:
    print 'isTaskRunning requires 2 args'
    sys.exit(1)
  pp.pprint(client.isTaskRunning(eval(args[0]),args[1],))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
